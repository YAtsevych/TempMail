version: "3"
services:
  db:
    image: postgres:15
    restart: always
    environment:
      POSTGRES_USER: ${POSTGRES_USER} # берёт из .env → batman
      POSTGRES_PASSWORD: ${POSTGRES_PASSWORD} # берёт из .env → supersecret123
      POSTGRES_DB: ${POSTGRES_DB}
    ports:
      - "5432:5432"
